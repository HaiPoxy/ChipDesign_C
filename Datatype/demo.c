#include <stdio.h>
int main()
{
    float x = 6.757;
    float y = 20.5043;
    float z = 77.45666;
    float result = x + y + z;
    // Tính tổng của x, y, z
    printf("Tong cua x, y, z: %.5f\n", result);
    return 0;
}

// lệnh [cd + tên file]: cd demo.c
// lệnh [cd ..] lùi lại 1 file5